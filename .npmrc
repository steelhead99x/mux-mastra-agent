# NPM configuration to prevent problematic native modules
build_from_source=false
cache_min=86400
prefer_offline=true
fund=false
audit=false
save-exact=true

# Skip optional dependencies
optional=false

# Ignore scripts that might cause issues
ignore-scripts=true

# Use npm cache
cache=/app/.npm
prefix=/app/.npm-global
